import Navbar from '../Navbar/Navbar';

// This class should exists as a wrapper for more elements if we need something else up there.
function Header() {
    return (
    <>
        <Navbar />
    </>
    )
}

export default Header;